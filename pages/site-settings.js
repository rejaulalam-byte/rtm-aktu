// ------------------------------------------------------------------
// Sitewide settings edited from the admin panel's Site Settings page
// (self-bhalani/settings-edit.php). Loaded on every page just before
// script.js, which applies them (applySiteSettings()): hides main-menu
// items switched off in menuVisibility, and shows/hides + relabels the
// top bar's "Admission Going On" ticker. The contact-form fields and
// subscribe texts are read by those features later (their recipient
// e-mail addresses are deliberately not exported here - public file).
// ------------------------------------------------------------------
// @admin-export-start siteSettings
// Everything between these two marker lines is regenerated by the admin
// panel (self-bhalani/settings-edit.php) from the database on every save -
// edit site settings there, not here.
const siteSettings = {
  menuVisibility: {
    home: true,
    'about-us': true,
    academics: true,
    admissions: true,
    'leadership-management': true,
    'international-applicants': true,
    contact: true,
  },
  admissionButtonOn: true,
  admissionButtonText: 'Admission Going On',
  contactFormFields: [
    {key: 'name', label: 'Full Name *', type: 'text', placeholder: 'Enter your full name', required: true},
    {key: 'subject', label: 'Subject *', type: 'text', placeholder: 'What is this regarding?', required: true},
    {key: 'email', label: 'Email Address *', type: 'email', placeholder: 'you@example.com', required: true},
    {key: 'phone', label: 'Phone Number', type: 'tel', placeholder: '+880 ...', required: false},
    {key: 'message', label: 'Message', type: 'textarea', placeholder: 'Write your message here...', required: false},
  ],
  subscribeLabel: 'Never Miss a Campus Update',
  subscribePlaceholder: 'Join Our Network by Email',
};
// @admin-export-end siteSettings
