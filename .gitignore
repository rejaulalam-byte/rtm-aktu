# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor/tooling
.vscode/
node_modules/

# Admin panel secrets
config/config.php

# Admin database scripts, seeds and backups - local only, uploaded via cPanel
database/

# Admin config (config.php holds DB credentials) - local only, uploaded via cPanel
config/

# Claude session scratch/export output
Claude outputs/
